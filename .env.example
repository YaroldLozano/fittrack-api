DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=Yarold

JWT_SECRET=change-this-to-a-long-random-string
JWT_TTL_SECONDS=604800

# Orígenes permitidos por CORS, separados por coma. Usa '*' para permitir cualquiera.
CORS_ALLOWED_ORIGINS=http://localhost:8100,http://localhost,capacitor://localhost,ionic://localhost

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_FROM_NAME=FitTrack

# IA para ajustar el entrenamiento del día (console.anthropic.com)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5
